{
  "groupRules": [
    {
      "flags": ["all"],
      "regex": "^[abc]",
      "subGroups": [
        {
          "regex": "^[bc]",
          "subGroups": [
            { "flags": ["named"], "regex": "^c" },
            { "flags": ["scripts"], "regex": "^b" }
          ]
        }
      ]
    }
  ]
}
