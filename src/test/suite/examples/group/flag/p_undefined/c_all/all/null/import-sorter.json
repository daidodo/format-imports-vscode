{
  "groupRules": [
    {
      "flags": null,
      "regex": "^[abc]",
      "subGroups": [
        { "flags": null, "regex": "^[bc]", "subGroups": [{ "flags": "all", "regex": "^b" }] }
      ]
    }
  ]
}
