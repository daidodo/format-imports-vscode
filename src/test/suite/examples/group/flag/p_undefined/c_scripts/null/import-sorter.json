{
  "groupRules": [
    {
      "flags": null,
      "regex": "^[abc]",
      "subGroups": [
        { "flags": null, "regex": "^[ab]", "subGroups": [{ "flags": "scripts", "regex": "^a" }] }
      ]
    }
  ]
}
